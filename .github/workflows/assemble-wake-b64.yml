name: Assemble wake b64
on:
  push:
    paths:
      - 'parts80/**'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  assemble:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Concatenate parts80
        run: |
          set -euo pipefail
          if ls parts80/morning.mp3.b64.p* >/dev/null 2>&1; then
            cat $(ls parts80/morning.mp3.b64.p* | sort) > morning.mp3.b64
            wc -c morning.mp3.b64
          fi
          if ls parts80/ambient.mp3.b64.p* >/dev/null 2>&1; then
            cat $(ls parts80/ambient.mp3.b64.p* | sort) > ambient.mp3.b64
            wc -c ambient.mp3.b64
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add morning.mp3.b64 ambient.mp3.b64 || true
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "assemble wake audio b64 from parts80"
            git push
          fi
